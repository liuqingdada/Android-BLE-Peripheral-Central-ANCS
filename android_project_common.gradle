apply from: "${androidModuleCommon}"

android {
    lintOptions {
        textReport true
        textOutput 'stdout'

        abortOnError true

        error 'Recycle','Wakelock','DrawAllocation'
    }
}
